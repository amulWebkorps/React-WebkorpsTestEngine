import { Typography } from '@mui/material'
import React from 'react'

const Required = ({lable}) => {
  return (
    <>
        <Typography sx={Head}>{lable}</Typography>
    </>
  )
}

export default Required